# What is knowingly wrong

There are things in this repository that look like an oversight and are a decision, and
others that are a real oversight but whose fix costs more than the bug. This page gathers
them so that nobody "fixes" one without first reading why it is still there — and so that
whoever finds one that is not here knows they can fix it without asking.

The sentence that governs this whole directory applies here more than on any other page:
**a gap that is written down is a decision, and one that is not is an oversight.**

**No test anchors this document**, and there are two rows that are anchored anyway: the five
colors below AA are held by `apps/web/app/styles/contrast.test.ts` with their figures, and
`verdicts.accepted` without a writer is held by `apps/web/lib/twin-wiring.test.ts`. The other
rows depend on somebody reading this. Everything asserted here was checked against the code
on 25-Aug-2026, the rows on model spend on 6-Sep-2026, those on the optional apps and on
the stylesheet on 8-Sep-2026, those on the handoff on 11-Sep-2026, those on its agent
channel on 12-Sep-2026 and those on the memory contract, its capture and its extraction on
14-Sep-2026.

There were three. **The third was `--color-smoke`, and on 8-Sep-2026 the owner answered it: the
gray goes two steps down, from `#6f6f6f` to `#6d6d6d`.** It was the only ink that cleared AA on
white and fell short on papers it lands on — 4.4093 on `--color-selected`, 4.4084 on
`--color-danger-soft-deep` — and `#6d6d6d` is the first value on the ladder that clears 4.5:1 on
all ten papers, at 4.5404 and 4.5395; `#6e6e6e` reaches 4.4733 and does not. Two steps of 255 is
a luminance change of 0.006, which is below the threshold of seeing it, so the most-written color
in the product went above AA everywhere without the interface moving. Its anchor did not go with
the row: the second list of `contrast.test.ts` is now **empty and still re-measured on every
run**, which is what catches the next ink that lands below on a paper that is not white.

## How to read the table

The column that matters is the last one. **Who decides** separates three things that are not
treated alike:

- **the user** — it is their decision, about the product or about how it looks, and it is not
  touched unless they ask for it;
- **decided** — already decided and written down with its reason: changing it is reopening the
  argument, not fixing a bug;
- **open** — nobody has decided it; it is pending work and it can be picked up.

## The visual, which the user decides

| what | where | why it is still like this | who decides |
| --- | --- | --- | --- |
| Five text colors below AA's 4.5:1: `--color-idle` 2.15 · `--color-dormant` 2.55 · `--color-live` 2.56 · `--color-faint` 2.58 · `--color-warn` 3.54 | `apps/web/app/styles/theme.css`, inventoried in `contrast.test.ts` | They are the house palette, not a slip: `--color-faint` is written as `text-faint` in 172 places and is the color of `.eyebrow`; the other three are the catalog's status dots, which are also used as a word. Raising their tone is a change of visual identity, decided by looking at the screen and not by fixing a test | **the user** |
| `--color-nogit` at 1.59:1, and outside the inventory above | `apps/web/app/styles/theme.css` | It is a dot, not a word: the markup writes `bg-nogit` and never `text-nogit`, and a colored dot answers to a different threshold | **decided** |

## The stylesheet, checked on 8-Sep-2026

Two things the sheet says it does and does not do. Neither shows up as an error, and neither
moves a pixel for anybody who has not asked for something.

| what | where | why it is still like this | who decides |
| --- | --- | --- | --- |
| Reduced motion still animates every jump to an anchor | `scroll-behavior: smooth` at `apps/web/app/styles/base.css:9` | `@media (prefers-reduced-motion: reduce)` flattens durations, and a duration does not reach `scroll-behavior`: the property is pinned on `html` with no override under the query, so every anchor of the project sheet still travels. The two spinners that shared this row were the same omission and are fixed: the query in `apps/web/app/styles/responsive.css` now switches `.is-spinning` and `.opening-spinner` off by name, the way `.nav-pending` (`apps/web/app/styles/catalog-extras.css`) already did — flattening the duration made them blur faster instead of stopping, and «Open everything» leans on one of them to say it is working | **open** |
| `--font-display` and `--font-sans` are the same string, and the markup writes `font-display` 26 times | declared in `apps/web/app/styles/theme.css`; written across the `.tsx` of `apps/web`, 26 times | Both read `var(--font-inter), ui-sans-serif, system-ui, sans-serif`, byte for byte, so `font-display` and `font-sans` render identically. The role exists and the face does not: whoever writes `font-display` on a heading is stating an intention the sheet does not honour, and reading the markup you would swear the product has two typefaces. The two ways out are opposite and only one of them is free — give display a real face, which is a typography decision and moves pixels everywhere at once; or delete the token and rewrite the 26 places to `font-sans`, which moves nothing and throws away the seam somebody left on purpose. Nobody has said which one it was | **the user** |

## The copy and the help, which really are bugs

| what | where | why it is still like this | who decides |
| --- | --- | --- | --- |
| With a single package, the English title says "1 dependencie" | `apps/web/lib/i18n.ts:3149`, `"packages.title": "{n} dependencie{s} across your portfolio"` | The `{s}` slot yields `""` when n is 1, and the singular of *dependencies* is not *dependencie* but *dependency*: the correct slot is `{ies}`. `plurals.test.ts` does not catch it because all it demands is that a message with a figure carry **some** inflection slot, and this one does. It is the number-at-the-end rule, again, and it only shows when n is 1 | **open** |
| The CLI help promises that `--install` writes `.mcp.json` | `apps/cli/src/lang.ts:38` and `:89` | It stopped being true when `installFor` started writing the file that **that** agent reads: `panoma agent-key Codex --install` goes to `~/.codex/config.toml`. It used to always write `.mcp.json` and answer "configuration written" — a success announced for doing nothing — and the code was fixed without fixing the help | **open** |
| The `/docs` page publishes the `--al-arrancar` flag, which the parser rejects | `apps/site/docs/docs-copy.ts:65` | It lives inside a `body`, and the flag check in `docs-copy.test.ts` walks the copy's texts and each block's `command`, but not the `body`. Fourteen tests green with the dead promise inside. `apps/cli/src/args.test.ts:176-182` explicitly proves that the alias no longer exists | **open** |
| The 401 HTML page is hard-coded in Spanish; the JSON of that same 401 goes in English | `apps/web/middleware.ts:195-257` | The fork by language is correct — the JSON is read by a machine — but the page is read by a person and it is the only surface of the product that does not go through the dictionary. Bringing it in would mean copying both texts by hand, because below that door nothing of Next can render: the layout queries the catalog, which is exactly what someone who has not got in cannot see. Debt noted in the file itself: "it fits; not today" | **open** |

## Spanish identifiers on machine surfaces

The rule is that canonical prose and identifiers go in English. These four identifiers break
it and are still there because changing them breaks something that is already out there.

| what | where | why it is still like this | who decides |
| --- | --- | --- | --- |
| The `#propuestas` and `#retrato` anchors on the project page | `apps/web/components/proposals-strip.tsx:46` and `apps/web/components/project-taste.tsx:48` | An anchor travels in the browser bar and in saved links; the other ten on the page (`#all`, `#summary`, `#md`…) are in English | **open** |
| The `?fijo=1` query parameter, which asks for the report without moving the read mark | `apps/web/app/api/today/route.ts:43`, sent by `apps/cli/src/next-command.ts` | It is a machine surface — probes and scripts — with a Spanish name. Changing it is two files at once, client and server | **open** |
| The `PANOMA_CUARENTENA_DIAS` environment variable | `packages/enrich/src/published.ts:71` | It is the only variable in the product with a Spanish name. Renaming it breaks the configuration of anyone who already has it set | **open** |
| Six comments say "the five sources" and there are four | `apps/cli/src/twin-command.ts:46` and `:638`, `apps/cli/src/twin-command.test.ts:234`, `packages/core/src/history/consent.ts:70` and `:316`, `packages/core/src/history/shared.ts:19` | `HistorySourceId` (`packages/core/src/history/inventory.ts:70`) has four members — `claude-code`, `codex`, `cursor`, `aider` — and `KNOWN_SOURCES` four keys. Another four comments say "the five" without the noun (`consent.ts:178`, `twin-command.ts:92`, `:296`, `:1089`). It is stale prose from when the count was something else; [twin.md](twin.md) already says four | **open** |

## Duplication and holes in the test net

| what | where | why it is still like this | who decides |
| --- | --- | --- | --- |
| Three byte formatters in the CLI, plus the web's | `apps/cli/src/render.ts:324`, `apps/cli/src/index.ts:514`, `apps/cli/src/twin-command.ts:2777`, and `apps/web/lib/format-bytes.ts` | The duplication is deliberate and written down: neither of the two in the CLI is exported, so reusing them means touching files that the task which brought the third one was not opening, and the copy was made "letter by letter so that merging the three is a deletion and not a decision". **It no longer is**: the third added a step with a decimal below ten megabytes — because "it weighs 3 MB and the cap is 3 MB" makes you think the command is broken — so merging them today is indeed a decision about which steps survive | **open** |
| The memory deliveries re-typed the same helpers module by module | the minute ledger (`memory-receipts.ts`, `memory-capture.ts`, `memory-patrol.ts`, `twin-learn.ts`), `scopeKeyOf` (four modules), the cursor codec of the paged doors (five `route.ts`, each matching its own error message), `readStdin` (`apps/cli/src/brief.ts` and `signal.ts`), `isPlainObject` and the opaque-id regex | Five agents wrote five deliveries in one day and none imported the other's helper. The verification of 14-Sep-2026 merged what had a home already — the local-day key (`localDayOf` of `@panoma/db`), `isUniqueViolation`, the handoff marker constant — and left these, because each merge is a decision the route tests pin by message: the cursor decoders throw a `TypeError` in one module and an `Error` in two, and every door's test matches its own sentence. Merging them is one module per helper and a pass over the tests that name the messages | **open** |
| The memory code and its four records cite the plan by section and task id, and the plan is under `private/` | `§8.2`, `T51`, `delivery D` and the like in `apps/web/lib/memory-*.ts`, `packages/db/src/memory-*.ts` and `docs/memory-*.md`; the plan is `private/memory-build-plan.md`, git-ignored | A reader of the public repository cannot resolve a section number, and the numbers age in silence the day the private plan is edited. Committing the plan, or an index of its section titles and task ids under `docs/`, is a decision about what the repository carries — the plan names the owner's research and the products it studied; rewriting every citation as the sentence it stands for is a day's work over 150 files. Until one of the two, the test titles keep the identifiers and this row says where they resolve | **the user** |
| `commands.test.ts` watches verbs and not flags | `apps/cli/src/commands.test.ts:42-50` | The file list is fixed already — since 25-Aug-2026 it enumerates `docs/` from disk, plus the four at the root — but what it compares are the verbs that `index.ts` dispatches. A dead flag written in any document under `docs/` is caught by nobody: the only flag check that exists looks at the `/docs` page, and not even all of it | **open** |
| Nothing compares `DOCS_COPY.catalog.views` against `PROJECT_VIEWS` | `apps/site/docs/docs-copy.ts:250` and `apps/web/components/project-views.ts` | Today the two lists of ten anchors agree. `project-views.test.ts` does not know about the page and `docs-copy.test.ts` does not know about `PROJECT_VIEWS`: it is the next figure that will go stale without anything failing | **open** |
| The published `dist/` lags behind `src/` | `packages/db/dist/client-B9SoVdKR.d.ts:2621` | It keeps a sentence that the source no longer has (`packages/db/src/schema.ts:553` rewrote it on purpose). It is not merely cosmetic: the packages import each other through their `dist`, so a stale `dist` means testing the old code without seeing it. It is fixed by rebuilding, and that is why the `build` line of the runbook in [testing.md](testing.md) is not optional | **open** |

## What looks half-done and is a decision

These do not get fixed. If you think something is missing, what is missing is the argument,
and it is here.

**`verdicts.accepted` has no write door.** The column exists with its three states — "have
not looked at it", "yes, that is me", "this does not represent me" — and the `GET` knows how
to filter by them, but no button or command marks anything. The writer that existed without a
door (`setVerdictAccepted`) was withdrawn, and `twin-wiring.test.ts` checks that it **stays
gone**: marking verdicts one by one would be the O(corpus) review queue this product walked
away from. Deciding lives one floor up, in signing and vetoing beliefs. Almost everything is
going to live in `pending` forever, and the filter says so instead of hiding it.

**Memory does not compact itself and cannot be edited.** That is not a technical limitation:
consolidating is discarding and writing again, so that the rewritten note passes through the
person's hands one more time. When the 2,000-character budget overflows, approval **is
refused** instead of trimming in silence. Any proposal to "summarize automatically" is exactly
what the product rejects.

**Memory ablation ships switched off.** `PANOMA_MEMORY_ABLATION` withholds memory on half the
visits so that whether serving it changes anything can be measured, and it ships off by a
written rule: withholding what the person curated does not happen unless the person flips the
switch. It also lives **only** in the agent channel; the dormant channel — the note that jumps
out when somebody is about to touch a path — is left out on purpose, because measuring
obedience by withholding the signal at the site of the accident would be measuring by causing
the accident. Since the memory contract (14-Sep-2026) a served visit writes the v2 offer as
the ledger's row and a withheld one answers the legacy shape without a contract, because the
contract carries the awake notes as required units and there is no half-contract to offer;
one row per visit either way, and with the switch off — the factory value — every visit takes
the offer path. **decided**

**Paid distillation does not run against a remote catalog.** Deferred on 6-Sep-2026 by the
owner, and not blocked by anything technical: the extraction queue was built for several
processes —a job is claimed under `LOCK TABLE memory_jobs` and finished or published under its
lease token, and both hold across them— and the distiller reads only the database and the
model, never a file on anybody's disk. What stops it is the bill. The key that would pay is the
**server's**, for every project it serves. The other half of the old reason is gone since
delivery B: the daily cap of the `memory` family is decided under a database lock by the
reservation, so N processes over one catalog no longer exceed it by N−1
([memory-capture.md](memory-capture.md)), and delivery D put `read` and `episodes` on the
same road ([twin-learning.md](twin-learning.md)) — and the capture pass, the paid extraction,
the Twin's learning and the publication outbox sit behind the same guards, for a reason of
their own: they open the person's disk, or write to it. Panoma
is local today, and a hosted key paying for everybody's extraction is a product decision that
has not been taken. Whoever takes it changes one line in each of three places, and this is the
whole list: the `if` around the start call in `apps/web/lib/db.ts:86`, and the early returns of
`runMemoryJobs` and `startMemoryWorker` in `apps/web/lib/memory-worker.ts`. The guards are
anchored: `apps/web/lib/memory-worker.test.ts` asserts that the queue does **not** drain under
`DATABASE_URL`, so removing one by accident turns it red. The deferral is **decided**; whether
panoma ever serves a remote catalog at all is **the user's**.

**Nobody has measured whether the memory improves a task.** The fifth phase of the
[audit of 6-Sep-2026](memory-audit-2026-09-06.md) asks for a held-out set of between 24 and 40
cases, a baseline measured with the memory withheld and a second measurement after new evidence
lands, over seven figures: evidence recall, exceptions preserved, correct abstention, task
outcome, corrections repeated, latency, and the size of the context served. None of that
exists. It is written down here on 6-Sep-2026 as future work instead of being started, because
it is a protocol with its own weeks —assembling the cases, running them twice, holding the
conditions still between the two runs— and not a change that fits inside a commit. The switch
the first half would need is already shipped and already off (`PANOMA_MEMORY_ABLATION`, in the
entry above). Until the protocol exists, every claim that this memory helps anybody is
supported by the suite, and what the suite proves is that the plumbing works: that a note is
delivered, that a sentinel falls, that a budget stops a call. That is a different claim, and
the difference is the whole point of this entry. **open**

**An image goes through no redactor.** It is the only thing in the whole product that leaves
the disk for a model unredacted, and there is no way to fix it: you cannot redact pixels
without looking at them. What is done instead is to say it out loud on all three surfaces,
and to require the operator key to ask for a capture of the inbox.

**A quiet Twin topic does not get synthesized again, and the CLI has no way to ask for it.**
With no new evidence there is no synthesis, and it is a measured defense: four passes without
a single new observation compressed the portrait — density fell from 4.6 to 3.2 and standing
beliefs from 19 to 15 — and every press of the button made it worse and cost money. It can be
forced by asking for the topic by name, but `--topic` is not in `KNOWN_FLAGS`, so today that
is only done by calling `POST /api/twin/synthesize` with `{"topic":"design"}` by hand. The
defense is **decided**; that the CLI has no lever for it is **open**.

**There is no `.env.example`.** An example file full of optional switches invites copying the
whole thing and pasting a key inside it. The argument is in [environment.md](environment.md),
and proposing to create it is proposing the opposite of what was decided.

**If the new folder is not cataloged yet, the memory goes with the row.** Moving a project
changes the sha1 of its path and therefore its `id`; before pruning, `rehomeMemory` moves what
a human or an agent wrote over to the heir — since 14-Sep-2026 the memory contexts too — but
only if the heir is **unique** and already in the catalog. The gap is declared in
`packages/db/src/ingest.ts` beside the move and in [memory.md](memory.md), and handing out
memory blindly would be worse than losing it.

## The memory contract, checked on 14-Sep-2026

Delivery A of the memory plan was built in one day and probed on the real host the same day
([memory-contract.md](memory-contract.md)). What it knowingly left open, and what it decided
with a reason, is here so that the next delivery neither finishes a row by guessing nor
reopens a decided one by mistake. The three contracts the plan listed as wrong before it —
the scope that fell to global when a name was missing, the recency cut of fifty ahead of the
search, and the seen file that decided exclusion across compactions — are closed by the
selector, the `unresolved` scope and the context generations, and have no row.

| what | where | why it is still like this | who decides |
| --- | --- | --- | --- |
| An `item` purge formerly kept the domain row | `purgeRound` in `packages/db/src/memory-purge.ts` | Closed on 14-Sep-2026 during the authorized memory review: exact current-revision domain copies are blanked along with photographs, check copies and outcome evidence. Legacy readers apply the barrier before query limits; managed files must finish cleaning first. Startup reopens older completed receipts that omitted these copies. A newer independent revision survives an old-revision purge | **closed** |
| A `source` purge formerly missed offers and lost its `delivered:` receipt entries | `resolveAffected` and deletion progress in `packages/db/src/memory-purge.ts` | Closed on 14-Sep-2026: source targets include offers of their native contexts; opaque context/offer coordinates persist across batches after native keys are blanked. External-copy entries accumulate rather than disappearing on the final re-walk. The regression uses three offers and a batch of one | **closed** |
| The photographs of a moved project's notes keep the old project id as scope | `rehomeMemory` in `packages/db/src/ingest.ts`; `memory_revisions.scope_ref` | Closed on 14-Sep-2026: a move photographs every moved note and commitment under the heir at `memory_rev + 1` with reason `scope` inside the ingest transaction; the earlier photographs keep the old id as history (pinned in `forget-root.test.ts`, §22.12.8) | closed |
| The signal's v2 road is opt-in and unmeasured | `PANOMA_SIGNAL_V2` in `apps/cli/src/signal.ts`; `profileFor` in `apps/web/lib/memory-hosts.ts` | Nobody has measured Claude Code's native ceiling for `additionalContext` in `PreToolUse`, so no host is verified for the signal, the door answers `409 unsupported_host` and the CLI falls back to the legacy `GET` inside its two seconds. Enabling it means a measured profile with a version and tests, never an invented maximum; the fixture of thirty notes of five hundred is the measure of the legacy shape, not of the host. On that road the notes the touched path triggers are the required core and the awake units travel after them as optional (`requiredOn` in `apps/web/lib/memory-delivery.ts`, plan §5.4); the plan also lets the signal omit the awake units while their complete reception is still accredited in that context instance, and that reuse waits for the same verified host with a receipt site, so today they are offered again on every signal | **open** |
| A `TASTE.md` over 1 MiB formerly reconciled as an empty portrait | `readTaste` in `packages/core/src/taste.ts`; `publishedFile` in `apps/web/lib/select-memory.ts`; `tasteLinesOf` in `apps/web/lib/taste-publish.ts` | Closed on 14-Sep-2026: the shared cap (`MAX_TASTE_BYTES`, exported) measures UTF-8 bytes everywhere — `readTaste` compared UTF-16 units until that day, so a file of two-byte letters twice the cap was parsed. The two readers that must tell "nothing" from "cannot be read" measure the file themselves before reading: the selector's `stat` answers `unreadable` and serves no criterion, the publication's snapshot answers `unreadable` and writes nothing, so the criteria are preserved unchanged. `readTaste` itself still answers the empty portrait and never throws — the review first made it throw, and that turned a big file into a 500 on `/twin`, the project card, `md sync` and the look ([twin.md](twin.md) says why the empty answer is the contract) | **closed** |
| One generated constraint name is a character too long, and PostgreSQL keeps sixty-three of it | `memory_dependencies_dependent_revision_id_memory_revisions_id_fk` (64) in `packages/db/migrations/0064_memory_contract_a.sql` and the `0064` snapshot | The foreign key exists and behaves as declared; the stored name is the truncated one, so a tool that compares names against the snapshot sees a phantom difference, and a `DROP CONSTRAINT` typed from the snapshot still works only because the server truncates the identifier the same way. Nothing to do for a catalog that has it; the day the key is touched, name it in `schema.ts` (`foreignKey({ name })`) so the snapshot and the database agree. Found on 14-Sep-2026 | **decided** |
| The control-character strip did not bump `MEMORY_RENDER_VERSION` | `printable` in `packages/core/src/memory-contract.ts`; the constant beside it | Since the review of 14-Sep-2026 every line and inline value drops C0 and C1 controls except LF and TAB before hashing, so the rendered text of an item that carried one changed; no legitimate rule carries one and delivery A is unreleased, and the selector and the read by id both read the constant, so the bump is one line the day a released catalog needs it | **decided** |
| The `cli` entrypoint and a compaction of Claude Code are `unknown`, a subagent is `no_delivery`, and Codex is `unsupported` | `VERIFIED_HOSTS` in `apps/web/lib/memory-hosts.ts` | The probe of 14-Sep-2026 ran `claude -p` inside the desktop app's shell (2.1.258), could not force a compaction, and six sessions were run of the plan's twenty. A row moves from `unknown` to `verified` when a person reads an offer's bytes at the site on that entry. A subagent is declared as a lack rather than probed: `SessionStart` does not fire for it, so no managed hook delivers there and a receipt in its transcript seals nothing; giving it a brief of its own is a hook Claude Code does not offer today. Codex stays `unsupported` for receipts until it has a hook that can carry a message and a record to read, which is its vendor's decision, not this repository's; delivery B does read its rollouts for typed facts and owner turns, which is capture and not reception ([memory-capture.md](memory-capture.md)) | **open**; the subagent and Codex: **decided, until the vendor moves** |
| The first brief formerly depended on granting capture | `installedHostVersion` in `apps/cli/src/brief.ts`; `POST /api/hook/context` | Closed on 14-Sep-2026: a `claude --version` query capped at 400 ms can report compatibility without reading a transcript. It permits an attempt only for the exact measured desktop brief version, 2.1.258, and writes no observed host, fact or reception. Version 2.1.266 has evidence for PostToolUse/Stop receipt parsing only; its brief remains unsupported. Missing executable, timeout and unknown version remain honest fallbacks | **closed** |
| Discovery lists two transcript shapes | `isClaudeCodeTranscript` in `packages/core/src/history/receipts.ts`; the sweep in `apps/web/lib/memory-receipts.ts` | `<folder>/<uuid>.jsonl` and `<folder>/<uuid>/subagents/<name>.jsonl`; this disk also has `<uuid>/subagents/workflows/<id>/agent-*.jsonl`, which neither the validation nor the sweep lists, so a receipt in one of those is never read. The two must change together, and the deeper layout was not in the spec | **open** |
| The observed invocations and receipts live in process memory | the ledger on `globalThis` in `apps/web/lib/memory-receipts.ts` | Only the program version survives a restart, on the source row; after a restart the matrix shows `invocation: unknown` and zero receipts until the next pass. A count that survived a restart would claim a memory the ledger does not have | **decided** |
| A same-size rewrite outside the anchor window is not detected | the 256-byte anchor in `packages/core/src/history/receipts.ts` and `memory-receipts.ts` | The plan's anchor is "around the cursor"; a prefix rewritten without touching those bytes or the size reads as the same generation. Hashing more is a cost per pass on every stream, and nobody has seen a transcript rewritten that way | **decided** |
| A remote catalog served by several processes would quarantine itself | `guardMemory` in `apps/web/lib/db.ts`, which runs regardless of `DATABASE_URL` | Each process keeps its own `memory-deletions.jsonl` under its `PANOMA_HOME`, so two of them over one catalog disagree on the journal id and the second one quarantines on `journal_mismatch`. The remote mode is deferred (the row above on paid distillation), and the day it is not, the journal has to live where the catalog lives | **the user** |
| Nothing measures whether a delivered contract was used | the reception in `serving_events`; the scale in [memory-scale.md](memory-scale.md); since delivery C the observations and incidents in `memory_outcomes` with `deliveredBefore` ([memory-checks.md](memory-checks.md)) | A reception says the bytes reached the context, unit by unit, and that is the most the record can say. Delivery C answered «what happened afterwards» as evidence — a check's look at the disk, an incident a violation opened, and `deliveredBefore`, `yes` only when a full reception of that revision precedes the look in the same context — and decided, with the plan (§23.4 4.2), that no row and no screen will ever say obeyed or ignored: an incident after a delivery is an occurrence with a verdict of the owner's, never a measurement of the agent. What is still open is the held-out set of the row above, which C did not write either | **decided** for the words; **open** for the measurement — the same row as "nobody has measured" above, with two more instruments |
| The unresolved scopes have a count and no list | `POST /api/twin/taste` answers `unresolved`; the selector counts `unresolved_scope`; nothing on `/twin` names the rows | Resolving one means renaming the project back or re-teaching the criterion under the current name, and the owner does it knowing the number and not the row. A list is a screen, and screens are the owner's to ask for | **the user** |

## The capture and the extraction, checked on 14-Sep-2026

Delivery B of the memory plan was built the same day as A, in the same tree
([memory-capture.md](memory-capture.md)). What it knowingly left open, and what it decided with
a reason, so that delivery D neither finishes a row by guessing nor reopens a decided one.

| what | where | why it is still like this | who decides |
| --- | --- | --- | --- |
| The version-2 acceptance formerly had no timestamp of its own | `setGrant` and `readConsent` in `packages/core/src/history/consent.ts`; `factsSince` in `apps/web/lib/memory-capture.ts` | Closed on 14-Sep-2026: `noticeAcceptedAt` records new or upgraded notices and survives unrelated saves. Receipt frontiers keep their generation and activation. Legacy grants without the field retain the conservative file-timestamp fallback; existing cursors never rewind. A caller test covers a stream created between acceptance and an unrelated consent save | **closed** |
| Historical cursors formerly lacked durable grant bindings and a project-extraction caller | `permission_snapshot` in migration `0071`; `backfillAuthorised`, `planWindows` and `executeBackfill` | Closed on 14-Sep-2026: confirmation records exact grants, generations and notices; both semantic processors use the matching historical facts cursor, manual origin and bounded intervals. Revocation or off/on invalidates the old approval; legacy cursors without bindings require a new explicit preview. Confirmation rejects an inode replacement before applying the preview's offsets | **closed** |
| Metadata readers formerly stopped at the first thousand rows | `allSources` and `allCursorsFor` in `packages/db/src/memory-sources.ts` | Closed on 14-Sep-2026: workers read stable keyset pages and discover older metadata beyond the first page; a 1,002-row test covers both helpers; the status document's cursors and sources and the extraction capacity report (`pendingBytes`, `oldestPendingAt`) read the same pages since the verification of that day, having kept a `limit: 1000` of their own until then. The complete metadata collection is still held in memory, so extremely large catalogs need a separate runtime measurement | **closed** for truncation; runtime scale **unmeasured** |
| A Codex continuation outside both bounded seed reads stays excluded | `peekHeader` in `packages/core/src/history/facts-codex.ts` | The first 64 KiB and the 64 KiB before the requested offset establish whether a copied prefix has ended. Without a native continuation marker in either, the reader keeps `carried` and excludes those turns until it sees proof; it never promotes imported text to owner evidence. A persisted marker coordinate would avoid this conservative loss | **open** |
| A parser bump reads on from the cursor under the new version and never rewinds | `memory_source_cursors.parser_version`; the pass's `skipped.parser_changed` | The cursor key has no parser version, so rows of two versions coexist at the same coordinates only through an explicit backfill (T87). Automatic re-reads on a bump need the version in the cursor key or a `reparseCursor` that resets `next_byte` to `allowed_from`, and both are the cursor table's rule to write | **open** |
| The grant door and the backfill door disagree on what extraction needs | `POST /api/twin/sources` (`memoryExtract` on top of any enabled capture); `authorising` in `apps/web/lib/memory-backfill.ts` (capture at notice 2 for either purpose) | A backfill writes facts, so it demands the notice that opens them; the grant door demands only a capture. A person with capture at notice 1 and extraction on is granted at the door and refused `consent_required` naming `memoryExtract` by the preview — right, and it reads as a contradiction. Making the grant door demand notice 2 for extraction changes what the extraction switch on `/twin` may show while the facts notice is unaccepted | **the user** |
| The Codex held turn at a window boundary can be counted twice | the header of `packages/core/src/history/facts-codex.ts`; `readTurns` in `apps/web/lib/memory-extract.ts` | A `response_item` turn held at the end of a read is emitted; if its event twin is the first record of the next window, the twin is a second turn with its own coordinates. It needs the read budget to land exactly between two lines the app writes together, the reader inside one read never counts a turn twice, and the extractor does not dedupe consecutive equal turns across a boundary: a duplicated fragment costs the window some units and never a wrong quote | **decided**, until it is seen on a real stream |
| The ledger row keeps the planned provider and model when the answer cannot correct it | `plannedModel` in `apps/web/lib/memory-distill.ts`; `completeReservation` in `packages/db/src/model-reservations.ts` | A reservation is written under what `resolveCredential()` says before the call, or `unresolved` when the configuration cannot be read; the answer corrects both on completion, and an uncertain call keeps the planned pair. A column for the reason of an uncertain call was refused: the ledger has no column for prose and should not grow one for a network error | **decided** |
| The status document formerly omitted capture skip reasons | `memoryStatus` in `apps/web/lib/memory-status.ts`; `lastCapturePass()` in `memory-capture.ts` | Closed on 14-Sep-2026: `queue.capture` carries the latest process-wide pass or `null`, and the CLI prints its stream/byte totals and nonzero skip reasons. This is an ephemeral diagnostic, not a durable per-project coverage claim | **closed** |
| A window's paid retries are bounded by the conversation before the ceiling | `PER_CONVERSATION_MAX` and `PAID_ATTEMPTS_PER_WINDOW` in `apps/web/lib/memory-extract.ts` | Two calls per scope key and day mean a third attempt of the same window on the same day is deferred to tomorrow as `conversation`, so the ceiling of three is reached across days and never in one. Pinned that way in `memory-extract.test.ts`, as the plan words it | **decided** |
| The purge round formerly left stream cursors active | `purgeRound` in `packages/db/src/memory-purge.ts`; `purgeSourceIdentity` in `memory-sources.ts` | Closed on 14-Sep-2026: the batch uses the source writer, which blanks the locator and revokes its cursors in the same transaction | **closed** |
| Nothing of delivery B was probed on a real host end to end | the two fixtures beside `facts.ts` and `facts-codex.ts`; the stunt-double provider of `memory-extract.test.ts` | The readers were run over the real transcripts of this disk counting only, and every path from the pass to the publication runs against PGlite with a fake `complete`; no paid call left this machine under the processor, and the two screens were photographed later that day. A section like A's real-host probe is owed the day the owner runs one | **the user** |

## The checks, the commitments and the case, checked on 14-Sep-2026

Delivery C of the memory plan was built the same day as A and B, in the same tree
([memory-checks.md](memory-checks.md)). What it knowingly left open, and what it decided with
a reason, so that delivery D and whoever comes after neither finish a row by guessing nor
reopen a decided one.

| what | where | why it is still like this | who decides |
| --- | --- | --- | --- |
| Fulfilment by checks formerly had no caller | `fulfilCommitment` with `actor: checks` in `packages/db/src/commitments.ts`; `writeLooks` in `apps/web/lib/memory-patrol.ts` | Closed on 14-Sep-2026: the patrol closes an open commitment only after every approved completion criterion passes at its current definition and subject revision in one fresh environment, with applicable conditions and no unknown or failed applicability check. The database verifies the evidence again. Partial or unknown results leave it open, and a later regression preserves its original resolution; no model infers completion | **closed** |
| An invalid stored predicate stops the reader rather than reading as "no condition" | `storedPredicate` in `packages/db/src/commitments.ts`; `candidatesOf` in `apps/web/lib/memory-patrol.ts` | A `conditions` column that does not validate — written past the door by hand or by a bug — makes `listCommitments` throw the shape error, so the patrol pass over that project stops and the commitments route answers 500 until the row is repaired; the commitment stays open and nothing is fulfilled (pinned on 14-Sep-2026 in `memory-patrol.test.ts`). Fail closed and blunt: a per-row degradation — the row read as unreadable, evaluated `unknown`, the rest of the project patrolled — is a type change on `CommitmentView.conditions` that every reader of a commitment would have to handle, and nobody has met such a row | **decided**, until a row appears |
| A decision's typed predicate formerly had no operator route | `setDecisionEpisodePredicates` in `packages/db/src/episodes.ts`; `POST /api/twin/episodes` | Closed for the API on 14-Sep-2026: the operator can send `conditionsPredicate` and `exceptionsPredicate` with `id` and `expectedRevision`. The closed body validates both trees, permits owner-origin decisions only, and refuses a moved revision. There is no dedicated decision predicate editor yet | **closed** for the API; editor **open** |
| Incidents are never linked by continuity | `openIncident` takes an `occurrenceId` in `packages/db/src/memory-outcomes.ts`; nothing computes the proof | The plan (§9.3) allows a disk observation and a later commit to be linked to one occurrence when the fingerprints and the continuity prove it, and forbids inventing it; no code computes that proof, so every incident is new. Building the proof means comparing the inspected fingerprints of two environments and a path of commits between two HEADs without running git, which is the evaluator's rule to write | **open** |
| `deliveredBefore` is `unknown` on every observation the patrol writes | `runPatrol`'s `contextId` option in `apps/web/lib/memory-patrol.ts`; `deliveredBefore` in `packages/db/src/memory-outcomes.ts` | A worker pass has no working context to ask about, and precedence is a fact about one context (§9.3); the db helper answers `yes` only with a preceding full reception in that context and never by time. The column exists for a caller that observes from inside a delivery, and none exists yet | **decided** |
| The environment is per item and per turn, and «the current environment» is the newest outcome of the project | `unionInspected` and `readEnvironment` per item in `apps/web/lib/memory-patrol.ts`; `lastPatrolEnvironment` in `apps/web/lib/select-memory.ts` | The completion criteria of one commitment share one `environmentId`, which is what «in one environment» needs; a note and a decision looked at in the same turn are two environments, and the selector reads the newest outcome as the environment to ask about, so a unit observed earlier in the same turn under another fingerprint reads as not observed there and travels `conditional`. Right by construction — a verification never transfers — and coarser than one fingerprint per pass, which would have to inspect every file of every item before writing any row | **open** |
| A global decision or criterion is observed once per project it is served to | `candidatesOf` and `readSharedRows` in `apps/web/lib/memory-patrol.ts` | The same subject revision, one environment per project root: its checks are looked at under every project's disk, and the checks and outcomes pages of a project show the looks made under its root only. Observing it once for all would mean choosing one root to speak for every project, which is exactly the transfer C03/T48 forbids | **decided** |
| The CLI formerly omitted checks, commitments and patrol status | `coverage.checks`, `coverage.commitments` and `queue.patrol` in `apps/web/lib/memory-status.ts`; `memoryStatusLines` in `apps/cli/src/memory-command.ts` | Closed on 14-Sep-2026: the terminal prints check counts, commitments by state, waiting projects and the latest process-wide patrol results. A missing pass is explicitly unobserved; an older server's absent fields remain omitted | **closed** |
| The `declared` half of a case is read two ways | `declaredBy` in `apps/web/app/api/memory/cases/route.ts`; `caseUnit` in `apps/web/lib/memory-delivery.ts` | The operator's door reads the sessions the assigned agent opened between the claim and the completion (20 sessions, 100 lines) and says `unknown` for a task nobody claimed; the agent's read has no claim to look at and reads the project's newest 100 activities. Both are what their caller may honestly read; making them one reading means giving the agent read the task's claim, which is the operator's record | **decided** |
| The `decided` half is the project's decisions, not the task's | `decisionsInForce` in the cases route; `caseUnit` in `memory-delivery.ts` | No row links an episode to a task, and the plan forbids a plausible story to fill the gap, so every task of a project shows the same decisions in force — a fact about the project, printed as such. A link would be a column on `decision_episodes` the owner fills, or a claim an agent makes, and neither exists | **decided** |
| The patrol's requests and its rotation live in process memory | `panomaPatrol` on `globalThis` in `apps/web/lib/memory-patrol.ts` | A restart forgets what was asked for and where the six-check window of an item stood; the next pass finds every project with checks anyway, oldest observed first, and reads each item from the top. A table for a request that the next heartbeat rediscovers on its own would be a row per minute for nothing | **decided** |
| Two shapes in one column, and the first is not migrated | `notes.sentinels`; `checksOf` and `isLegacy` in `packages/db/src/memory-checks.ts`; `isLegacyAnchor` in `apps/web/lib/sentinels.ts` | A first-generation anchor is `legacy:<index>` on read, purpose `grounds`, and cannot be edited, only re-anchored whole by approval; the second generation sits beside it. Migrating the column means assigning ids to anchors the customs rewrites on every re-approval, which would make them stop being the customs' | **decided** |

## The Twin that learns on its own, checked on 14-Sep-2026

Delivery D of the memory plan was built the same day as A, B and C, in the same tree
([twin-learning.md](twin-learning.md)). What it knowingly left open, and what it decided with
a reason, so that whoever comes after neither finishes a row by guessing nor reopens a decided
one. The manual synthesis route still accepts one new observation; the automatic road uses
the grouping thresholds recorded below.

| what | where | why it is still like this | who decides |
| --- | --- | --- | --- |
| The automatic synthesis triggers formerly used only a changed fingerprint | `synthesisDue`, `planTwinBatches` and `runClassify` in `apps/web/lib/twin-learn.ts` | Closed on 14-Sep-2026: a persisted topic receipt records evidence revision ids and the external context hash. New evidence waits for three independent families, an explicit correction or choice with a known referent, or an observation aged 24 hours. Removed evidence or a changed owner/context input permits regeneration; identical input does not. Tests distinguish three families from three turns of one session | **closed** |
| A session end does not shorten the thirty minutes | `STABILITY_MS` in `apps/web/lib/twin-learn.ts`; `POST /api/hook/session` | The plan says a session end may bring the free review forward; the hook wakes the worker and the planner runs, so a batch that names nothing is closed for free at once, but a batch with a referent still waits for the quiet or the four hours. A shorter window on a closed stream is one condition in the planner and one more thing the notice would have to say | **open** |
| An open exchange is not a rule of its own | `freezeBatch` in `apps/web/lib/twin-learn.ts` | The plan blocks promoting a criterion whose human turn has no record after it, without discarding the batch's other records; here a turn read whole is admissible whether or not an answer followed, and only a record the end of the file cut through waits. Telling the two apart needs the reader to say whether an assistant record followed, which the owner-turn readers do not report | **open** |
| `teach:` is a reserved origin with no writer | `TEACH_ORIGIN_PREFIX` and `originKind` in `packages/db/src/twin.ts`; `teachBelief` in `apps/web/lib/teach.ts` | Direct teaching writes a signed belief and no observation, so no row carries a `teach:` origin and a lesson typed into a transcript founds a `case`. The prefix exists so that a teach gesture can found a family the day the teach form writes an observation; whether it should — a signature already publishes without a quorum — is the owner's call | **the user** |
| The Twin backfill purpose formerly had no execution path | `authorising` and `executeBackfill` in `apps/web/lib/memory-backfill.ts`; `planTwinBatches` in `apps/web/lib/twin-learn.ts` | Closed on 14-Sep-2026: an explicit preview and confirmation require capture notice 2 and `twinAutoLearn` for the same scope, create bounded `facts` and `twin_extract` cursors, and retain the approved grant bindings. The learner uses those cursors with manual origin and validates the approval before reading, sending and publishing; ordinary cursor positions and copied-text exclusions remain unchanged | **closed** |
| The legacy `TASTE.md` writer remains for compatible clients | `POST /api/twin/taste`: the legacy body writes inline inside its transaction, the version-2 body plans and runs the outbox | The Twin screen now sends version 2. The legacy route shares the catalog's file-write exclusion and deletion guard, but still writes inside its inherited transaction and does not gain the outbox's durable recovery receipt. Removing that compatibility route is a separate protocol decision | **open** for legacy crash recovery |
| The watcher formerly wrote the managed block outside the outbox | `syncManagedDoc` and `syncProjectDoc` in `apps/web/lib/md-sync.ts`; `runPublication` and `cleanDeletionFiles` in `apps/web/lib/taste-publish.ts` | Closed on 14-Sep-2026: the watcher plans publication only; publication and deletion cleanup share file-write exclusion. Explicit document sync also shares that exclusion, checks the deletion generation and compares the file immediately before writing. Recovery visits eight managed targets per heartbeat with a process-local cursor; it resumes after a crash between TASTE and managed-file publication. External editors do not participate in the lock, so hash checks and readback remain necessary | **closed** for the server's competing writers |
| The publication generation is conservative per project | `publicationState` in `apps/web/lib/taste-publish.ts`, over `listJobs` | The number is 1 plus the `taste_publish` jobs ever planned for the target's project, and `JobView` hides the scope key, so a project's `AGENTS` and `CLAUDE` targets move each other's number. A flip refused for it is a re-read and never a lost gesture, which is the side to fail on | **decided** |
| The fingerprint is global per topic, and a scope gesture that coincides with new evidence costs one extra synthesis | `topicFingerprint` and `lastSynthesisHash` in `twin-learn.ts`; `synthesis_passes` has no scope column | Two scopes that trigger one topic pay once, which is the intended economy; the refine's `edit` revision replaces the owner's `scope` one in the hash, so the next wake sees a moved fingerprint and pays one synthesis that changes nothing. Rare, bounded to one call, and cheaper than a scope column and a pass per scope | **decided** |
| Fairness between scopes is one open batch per scope, not a round robin | `distillInFlight` in `twin-learn.ts`; `claimJob` in `packages/db/src/memory-jobs.ts` | The queue claims by processor and oldest due; a per-scope turn would need putting a claimed job back, which the jobs API does not do, and the per-scope maximum of 4 attempts a day already stops one scope from spending the day | **decided** |
| Provenance of a predicate leaf is the photograph's authority, not a marker | `criterionPayload` in `packages/db/src/memory-revisions.ts`; core's closed predicate envelope | The D-spec asked for a literal `provenance: "owner"`; the envelope refuses unknown keys and the photograph is built from the columns, so what pins the owner's trees is `owner_instruction`/`signed` and a model's `inference`, with a bare signature clearing the model's. A marker per leaf is a schema decision over the envelope shared with decisions and commitments | **decided** |
| Generalisation across families of projects stays deferred | plan §10.2 | A private criterion reaches another project only as the abstraction the owner widened by a scope gesture, and never with its evidence (D08); no policy generalises one on its own. The plan defers it and this delivery keeps it deferred; it is not confused with recovering a global criterion the owner already approved | **the user** |
| The screens of delivery D are told in [web-app.md](web-app.md) and not verified here | the histories card, the Twin screen's criterion block, the learning block and the publication state | The record describes the contracts the screens read — `queue.twin`, `GET /api/twin/taste`'s `publication` and `revisions`, the version-2 body — and the two rules they keep; what landed is the screens page's to say, and a screenshot in `.panoma/shots/` its evidence | **open**, until web-app.md says what landed |

## The storage quota, checked on 14-Sep-2026

Delivery E of the memory plan (plan §25.3, T39) was built the same day, in the same tree
([memory-capture.md](memory-capture.md), «The storage quota»). What it knowingly left open,
and what it decided with a reason.

| what | where | why it is still like this | who decides |
| --- | --- | --- | --- |
| Physical disk capacity is reported separately from logical memory usage | `memory-disk.ts`, `spendReport` and `memoryStatus.coverage.disk` | Closed in the implementation review: `statfs` reports available bytes, low capacity below 256 MiB, full capacity at zero, and unknown on failure or a remote catalog. It is an observation, not a guarantee that the next write succeeds. | closed |
| The Spend screen edits both storage quotas | `SpendControls`, `patchSpendSettings`, `/api/spend` and `spendReport` | Closed in the implementation review: catalog and per-project MiB limits round-trip through the same form, validate atomically, preserve an omitted scope and expose environment overrides as disabled fields. Resetting a field restores its default; storage and spending remain separate. | closed |
| Batch processors formerly paid without reserving publication capacity | `reserveJobStorage`, `stageJob`, `publishJob` and migration `0070`; callers in `memory-extract.ts` and `twin-learn.ts` | Closed on 14-Sep-2026: the leased job reserves 256 KiB before sending, staging converts reserved bytes to retained content, and final publication checks fresh limits in the same transaction as domain writes. An over-limit publication rolls back while keeping its paid stage and capacity; a later claim publishes without another provider call. Cancellation and obsoletion release capacity, reconciliation counts it, and an outbox retains, of a completed publication's photograph, the units alone — id, revision and line — that a purge needs to remove the copy from the file, dropping the rendered text and crediting it (the whole photograph was kept for ever until the verification of 14-Sep-2026) | **closed** for batch processors |
| The reconciliation is a process memo | `lastQuotaReconcile` in `apps/web/lib/memory-quota.ts`; `reconciledAt` and `drift` on `coverage.quota` | The counters are rewritten in the table; when the last reconciliation ran and what it corrected live on `globalThis`, so a restart says `null` until the next local day's heartbeat. A column on `memory_usage` for the instant, and a row for the drift, would survive the restart at the cost of a shape nobody reads twice | **open** |
| A job deferred as `quota` is claimed again every quarter of an hour | `QUOTA_RETRY_MS` in `apps/web/lib/memory-quota.ts` | A pause ends only when the owner purges something or raises the limit, so a midnight retry would be theatre and a minute's would churn the row; a purge is honoured within fifteen minutes, and the deletion itself was never held back. Waking the worker from the purge route would make it immediate | **decided**, until the wait is felt |
| The capture pass memoises nothing for a stream its project's quota skipped | `visit` in `apps/web/lib/memory-capture.ts` (`skipped.quota`, no `state().skipped` entry) | The stream is stat-ed and, when the catalog has not resolved it, its head is read on every pass while the project stays at its limit — bounded by the pass budgets and the minute ledger, and the reads stop the heartbeat after the counter comes down. A memo keyed by the counter would save the reads at the cost of a second reason to forget it | **decided** |
| A budget-`paused` spend and a quota-`paused` catalog are two different pauses with one word | `spend.json`'s `paused` (every cap reads as zero) and `quotaState().paused` (the catalog is at its limit) | The first is the owner's switch on spending, the second a full counter; the screens say each with its own sentence, and the CLI's `memory status` names the quota's. The word is the plan's in both places | **decided** |

## The evaluation the plan asks for before tuning, checked on 14-Sep-2026

Plan §15 and §24.5 («Evaluación en cada entrega», T59) ask for a frozen corpus — 32 development
cases and 40 evaluation cases, in Spanish, English and mixed, over at least four project contexts,
with the expected answer coming from verifiable fixtures or human judgement and never from the
system evaluated — and an accumulated budget per task (8,000 memory tokens and 32 KiB of
responses, two retrieval pages and four full reads, repeated core and revalidations counted).
The implementation review added the frozen fixtures and task-wide budget account described below.
They complement the existing A/B arm switch; neither is a measured model-answer pilot.

| what | where | why it is still like this | who decides |
| --- | --- | --- | --- |
| Model-answer and human-pilot evaluation remain to run | `apps/web/lib/fixtures/memory-evaluation/`, `memory-corpus.test.ts`, `memory-evaluation.ts` | The review froze 32 development and 40 evaluation fixtures before execution, with withheld expectations and eight project contexts. The integration harness measures retrieval, unit preservation, abstentions, scope and accumulated response cost. The offline byte counter is a conservative proxy, not a provider tokenizer. A fixed model, its tokenizer, a comparable baseline and independent answer judgment are still needed for the pilot threshold; synthetic templates do not prove real-world independence. | implementation: run the model pilot with an explicitly chosen provider and labeled material; no owner-authored corpus is inherently required |

## The dependencies that travel, checked on 6-Sep-2026

An outside scanner —socket.dev— read the published `panoma@0.1.9` and found ten advisories in
three transitive packages plus two critical ones in `next` itself. All of them were real, all of
them are closed in 0.1.10, and none of them could have been found from inside this repository.
Chasing them turned up the larger fact: 91 of the 117 packages inside the tarball were an HTTP
transport that never runs, and they left too. What follows is what the episode left behind. The
record of the fix is in [release.md](release.md).

| What | Where | What is known | Who decides |
| --- | --- | --- | --- |
| Nothing here watches the dependencies that travel inside the tarball | `pnpm-workspace.yaml` `overrides`; `packages/core/src/ecosystems/npm.ts`; `.github/workflows/` | On 6-Sep-2026 socket.dev found ten advisories against `panoma@0.1.9` and every one of them was real: `next` 15.5.23 with two **critical** remote executions closed in 15.5.24, four high ones in `fast-uri` 3.1.5, four in `postcss` 8.4.31 and two in `qs` 6.15.3. Nothing in this repository could have found them. There is no `pnpm audit` and no advisory job in CI —the five workflows are `apps`, `apps-probe`, `cla`, `package` and `tests`— and panoma's own scan cannot see them by construction: `npm.ts` builds its list from what a manifest declares and reads the lockfile's `importers:` only, so a transitive dependency is invisible to it. That is why the catalog's own «0 with security advisories» is not a clean bill for the tarball. The material is already there to fix it: `app/BUILD-INFO.json` lists the exact version of all 26 packages that travel — they were 117 until the MCP SDK's HTTP transport stopped travelling the same day, and a query against OSV over that list is a few lines. What it would still miss is exactly what bit hardest: the two `next` advisories are only in Vercel's repository, and neither OSV nor `npm audit` know them | **open** |
| A scanner reads packages out of manifests nested inside the vendored ones | `check-package.mjs`, the nested-manifest census | Closed on 6-Sep-2026 and left written down because the shape will come back. `fast-uri` publishes a `benchmark/` folder carrying its own manifest —`{"name": "benchmark", "version": "1.0.0", "dependencies": {"tinybench", "uri-js"}}`— and socket.dev counted **three** packages that are not in the tarball, one of them published by an npm account that no longer exists. It left with the MCP SDK's HTTP transport rather than by being pruned. What could not be done is the obvious rule: of the 120 manifests nested inside vendored packages every one holds something up —111 under `next/dist/compiled/`, whose nested `main` is the only way Node resolves them and 30 of which have no `index.js` at all, and 9 nameless markers and `main` redirections, one of them a folder holding nothing but its manifest— so deleting nested manifests breaks the package twice over. The guard counts instead of deleting, and refuses at `prepack` | **decided** |

## The model spend, checked on 6-Sep-2026

The day the seven caps moved into one module and the Spend screen was built, every organ that
pays was read again ([budgets.md](budgets.md)). What follows is what was found and not fixed,
with who decides each. The SQL is written to be run against the catalog before deciding, and
none of it has been run yet.

The first row is the exception, and it stays here rewritten instead of deleted: the owner
took that decision the same day and it was built, so the row is no longer pending work — it
is an argument to read before anybody proposes shrinking a capture by default, and it carries
the one thing about it that nobody has measured.

| what | where | why it is still like this | who decides |
| --- | --- | --- | --- |
| Screenshots can now be fitted before the look, and it is the owner who says so — **decided and built on 6-Sep-2026** | `packages/core/src/image.ts`, `fitForLook` and `readCeiling` in `apps/web/lib/look.ts`, `shots` in `apps/web/lib/spend-settings.ts`, the two ceilings in `packages/core/src/screenshot.ts` | The figures that opened this row still hold: an image costs ⌈w/28⌉ × ⌈h/28⌉ visual tokens, capped at 1,568 on the models that resize to 1,568 px on the long edge and at 4,784 on Claude 4.7 and later, which accept 2,576 px; 1,920×1,080 goes from 1,560 to 2,691 tokens and 3,840×2,160 from 1,560 to 4,784. What was refused was not shrinking, it was shrinking **in silence**, and that is what the build honours: the Spend screen carries the choice (`full`, the factory value, or `fit` to a long edge of 1,568 px), `fitScreenshot` does the arithmetic with `node:zlib` alone —no image library entered the repository— and the size is said before spending and again on the receipt, across the three doors. The same day the size cap was split in two, because one number had been answering two questions: `MAX_SCREENSHOT_BYTES` (3,500,000) is what a provider accepts and now governs only **what travels**, measured on the bytes `fitForLook` hands back, while what may be **opened off this disk** is the choice's to pick — `MAX_FITTABLE_BYTES` (16,000,000) under `fit`, the provider's number under `full`. Before the split a six-megabyte capture was refused when the file was opened, which is another act with another price, and there was nothing to be done about it; now there is, and it lands far under the cap. What it still refuses is to guess: a JPEG, a palette or interlaced PNG, a capture already small enough, unreadable bytes or one over `MAX_FIT_PIXELS` (40,000,000, this machine's memory and not a provider's) travel whole with the reason named — and when what travels whole is still over the provider's number, the surface refuses with the size and the reason rather than buy a paid error about encoding. **What remains open is whether a fitted capture costs the critic a finding** — a caption in very small type, a one-pixel misalignment — because nobody has measured it: there is no comparison of the findings on the same screen at both sizes, which is why `full` is still the factory value. The whole record is in [budgets.md](budgets.md) | **decided** |
| An unusable paid answer is filed as an abstention, so the double's coverage charges the model's failures to the owner's taste | `shadowDraft` in `apps/web/lib/consult.ts`; `doubleReport` in `packages/db/src/consultations.ts` | `shadowDraft` writes `{ abstained: true }` both for an explicit `{"abstain":true}` and for an unusable answer —bad JSON, an invented citation, a second cut after the one retry— and `coverage = drafted / (drafted + abstained)` cannot tell them apart. Proposal, not taken because it needs a migration: a status value `unsupported` on `consultations.status` (a text column with no `CHECK` today; the schema comment would read `drafting · drafted · abstained · unsupported`); `draftConsultation` accepts `{ unsupported: true }` and sets `status = 'unsupported', draftedAt = now()`; `shadowDraft` passes it when `receipt.reason === 'unsupported'`; `doubleReport` adds `unsupported: count(*) filter (where status = 'unsupported')::int`, keeps `coverage` over drafted and abstained only (unsupported rows measure the model, not the owner) and adds `unusable = unsupported / (drafted + abstained + unsupported)`; `pendingConsultations` ignores the new status (it cannot be labelled) and `staleDrafting` never re-drafts it (the length retry already covers the one recoverable case). Measure first: `select status, count(*) from consultations where created_at >= now() - interval '30 days' group by 1;` cannot tell them apart today, which is the point | **open** |
| An agent that asks the same question twice pays two `ask` calls | `shadowDraft` and `recordConsultation`, `apps/web/lib/consult.ts` | Measure repeats with `select project_id, question, count(*) from consultations group by 1, 2 having count(*) > 1;`. Candidate: if a `drafted` row with the same `project_id` and `question` exists within `STALE_MAX_DAYS` and has no `vetoed` verdict, copy its answer and belief ids without a call —a second row still lands so the queue and the exam see the question. Not done because it hides a change in beliefs between the two askings | **open** |
| The extractor sends `assistantContext` uncapped | `narrativeLine` in `apps/web/lib/episode-learning.ts` | `row.context` goes whole into the batch and only `BATCH_CHAR_LIMIT` (24,000) bounds it, so one long agent reply can occupy a whole paid call while it can never be cited. Measure with `select count(*), percentile_cont(0.5) within group (order by length(context)) as median, max(length(context)), count(*) filter (where length(context) > 2000) as long from narratives where context is not null;`. Candidate: cap it at ~1,500 characters (the head) with a `truncated` marker; the prompt already says context is never testimony | **open** |
| Beliefs with zero lexical overlap still travel with the double's question | `selectAskBeliefs` and `fitBeliefs`, `apps/web/lib/consult.ts` | The ranking is by term overlap, but the 5,500-character envelope is filled with relevance-0 beliefs too, so most of the double's ~1,800 input tokens are beliefs the ranking already judged unrelated. Candidate: drop relevance-0 beliefs when at least one belief scored above zero (keep them all when none did, so the model abstains on evidence and not on an empty list). Risk: lexical zero is not semantic zero —synonyms, a Spanish question over English beliefs— and a trimmed belief cannot be cited. Measure before deciding: for the drafted rows of the last 30 days, how often a cited belief shares no term with the question; that needs a script over `consultations.belief_ids` (jsonb) joined to `beliefs.statement` through `terms()` in `apps/web/lib/lexical.ts`, because the tokenizer is JavaScript and there is no pure SQL for it. It is a measurement bias before it is a saving | **open** |
| The project card shows the 20 newest consultations and the sweeper drafts the 5 oldest stale ones, so a draft can be paid for and never shown | `listProjectConsultations` (limit 20), painted at `apps/web/app/(app)/p/[slug]/page.tsx:169`; `staleDrafting` (limit 5) in `packages/db/src/consultations.ts` | After the 30-day bound, a `drafting` row inside the window but older than the project's 20 newest consultations is still drafted and never painted. Candidates: restrict `staleDrafting` to ids among the newest 20, or list unlabelled drafts first on the card. Measure: `select project_id, count(*) from consultations where status = 'drafting' and created_at >= now() - interval '30 days' group by 1;` against each project's total in the window | **open** |
| A review queue can fill while a paid distillation runs | `apps/web/lib/memory-distill.ts`, `stageJob` and `publishJob`; `memory-distill.test.ts` | Closed for both `project_extract` and `legacy_session`: validated candidates are staged before publication, and a full queue rolls the transaction back. The next claim revalidates the session, scope and deletion generation and publishes the saved answer without paying again. Legacy jobs also reserve storage before sending and check current limits at commit; quota delays preserve the same answer. Claims with expired leases cannot pay or publish, and cancellation or a purge of input memory fences their output | **closed**, 14-Sep-2026 |
| A project with no repository keeps no plan for «Open everything» | `saveOpenPlan` in `packages/db/src/queries.ts`, which returns `false` when `identity` is null; the dialog in `apps/web/components/open-all.tsx` | The same limit as the row above and the accounts: `decisions` hangs from the stable identity. The dialog says it, what is ticked opens by key on that click, and commands and custom links are disabled there. It is the same path-derived key that would fix it, and the same reason it is not a slice's call | **the user** |
| A project with no repository pays for its description and opinion and keeps them only while the page is open | `saveAiSummary` and `saveMdReview` in `packages/db/src/queries.ts`, silent when `identity` is null; `apps/web/app/(app)/p/[slug]/page.tsx` reloads with `initial: null` | The paid paragraph is written to the ledger and returned with `saved: false`; the card says so, and after a reload the button is offered again as if never pressed. In the owner's documented catalog that is 32 of 76 projects. The fix is a path-derived key for repository-less projects (`path:<sha1 of root>`) so `decisions` can hang from it, and it changes what identity promises —stable across moves and renames—, which is why it is not a slice's call | **the user** |
| No prompt caching on the Anthropic path | `packages/ai/src/complete.ts`, the `anthropic` family | The stable prefixes of the prompts measure 33-448 tokens, under the smallest cacheable minimum (512 tokens on the Opus 5 class; 1,024 on Opus 4.8, Sonnet 5 and Sonnet 4.6; 2,048 on Opus 4.7; 4,096 on Opus 4.6 and Haiku 4.5), so a `cache_control` marker would create nothing. The one exception is distill's ~1,066-token instruction block, which clears the 512 and 1,024 minimums and not the rest. Before any of it the ledger would need `cache_creation` and `cache_read` columns, or the receipt would count a cached call as a full one | **open** |
| No `effort` or thinking control on the Anthropic path | `packages/ai/src/complete.ts`; `@anthropic-ai/sdk` 0.71.2 | The installed SDK has no `output_config` typing, so the parameter would travel untyped. Measure first: `output_tokens` against the visible answer on the `classify` and `distill` rows of the ledger, to see whether anything is being spent on reasoning the caller never reads | **open** |
| The memory distiller pays for sessions that cannot yield memory | `distillSession` in `apps/web/lib/memory-distill.ts`; its first brake asks for two activities | Two one-line records are enough to pay a call today. Measure first, the complete jobs whose receipt proposed nothing over sessions whose activities carry no details: `select count(*) from memory_jobs j where j.status = 'complete' and (j.receipt->>'proposed')::int = 0 and not exists (select 1 from agent_activities a where a.session_id = j.session_id and a.details is not null);` | **open** |
| One new observation still permits an explicitly requested synthesis | `POST /api/twin/synthesize`, the freshness check against `synthesis_passes` | The manual route retains its contract: the operator can request synthesis after one new observation. Automatic synthesis now groups evidence by independent families, an explicit correction or choice with a referent, or a 24-hour wait ([twin-learning.md](twin-learning.md)). Applying that wait to an explicit request would be a separate behavior change | **decided** for the manual route |
| The look prompt is Spanish while every other prompt is English | `systemFor` and the prompt builder in `apps/web/lib/look.ts` | About 2,400 characters of Spanish system and framing text, an estimated 100-250 tokens per look more than the English equivalent. It is the standing exception to "a machine reads English", and it is decided rather than pending: the critic judges with the person's own sentences, in the language they wrote them, and the frame around them was written in the same voice; the header of `look.ts` is where the argument lives | **decided** |
| The public `/docs` copy still says "four daily caps" and names the variables as the only control | `apps/site/docs/docs-copy.ts:477`, `:534`, `:611-630`, `:822` | `apps/site` is not changed without asking. The page should mention the Spend screen and `PANOMA_REHEARSE_BUDGET`, `PANOMA_EPISODE_BUDGET` and `PANOMA_CARD_BUDGET`; until it does, `docs-copy.test.ts` only keeps it honest about the variables it names existing in [environment.md](environment.md) | **the user** |
| `panoma ai ask` is outside the ledger | `apps/cli/src/ai-command.ts` | It runs `complete()` in the CLI process without the server, so there is no catalog to write a row in: the call shows in no receipt and no cap holds it back. It is the connection test, one question typed by hand; routing it through the server would make "does my key work" depend on the catalog being up | **decided** |

## The optional apps, checked on 8-Sep-2026

The design that brought them —`apps-design.md`, outside this repository— closes with six things
it says must be measured rather than assumed, and two questions for the owner. They are here so
that "it works on this laptop" does not quietly become the answer to any of them.

| what | where | why it is still like this | who decides |
| --- | --- | --- | --- |
| The app manager on Windows, which had never run there | `packages/apps/src/process.ts`, `environment.ts`, `manager.ts`; `.github/workflows/apps.yml` | Closed on 8-Sep-2026, and written down because the reasoning behind it is the kind that returns. The environment an app inherits names `SystemRoot`, `windir`, `ComSpec`, `PATHEXT`, `TEMP`, `TMP` and the two app-data directories, because without them Node cannot initialize there, `cmd.exe` cannot be found for the `npm.cmd` wrapper `resolveExecutable` builds, and a browser has nowhere to write — and that was reasoning, not a measurement, on a laptop where none of those names exist. The first `apps.yml` run answered it: 19 files and 168 tests green on `windows-latest` in 44 seconds, `install.test.ts` among them with a real `npm install --prefix` through the guardian against a loopback registry, and `process.test.ts` counting the processes a killed host leaves behind. A workflow that has never run is a plan; this one has run | **decided** |
| Chromium travels whole when the shell may be enough | the `browser` requirement of `@panoma/video`'s manifest, `approxMB: 550` | Video's own CI measured 356 MB for `chromium` and 197 MB for `chromium-headless-shell`. If the rasterizer and the capture work with the shell alone the requirement halves and the consent sentence changes — and that sentence is now read from the manifest on both surfaces, so changing the figure is changing one file. Nobody has pointed `PLAYWRIGHT_BROWSERS_PATH` at a cache holding only the shell and run the suite | **open** |
| What a progress write per second costs the rest of the server | `appProgressSink` in `apps/web/lib/app-jobs.ts`, `queueWrite` in `packages/db/src/queue.ts` | Coalescing is at one second and a render lasts minutes, so the write queue takes one small write per second for as long as it runs. Whether that is felt by anything else was never measured; the lever, if it is, is the interval | **open** |
| A repository with two copies in the catalog is never told which folder its production used | `appProject` in `packages/db/src/apps.ts`; `VideoProduction` in `apps/web/components/video-production.tsx` | The behaviour is right —the screen sends the project that was opened and the job records it, so the ambiguity never reaches the app— and only the sentence that would explain it is missing. Saying it needs a count of the rows sharing an identity, which is a query nothing else asks for. The sentence was written and then removed rather than left in the dictionary unused | **open** |
| The end-to-end run is in no CI | `apps/web/lib/apps-e2e.test.ts` (`PANOMA_E2E=1`), `.github/workflows/apps.yml`, video's `package.yml` | The manager itself now has CI: `apps.yml` runs the app suites on Windows and Linux on every pull request that touches them, and the weekly matrix runs them inside the whole suite, so the guardian, the npm lookup, the containment and the child's environment are measured where they differ. What is still not measured anywhere is the end-to-end lab, and it needs two things a workflow cannot invent: the real `@panoma/video` tarball, which `package.yml` already uploads as an artifact **in the other repository**, and a product to film, which does not exist in this one. Reaching across needs a token for a private repository, and shipping a fixture product is a decision about what this repository carries | **the user** |
| The first production's defaults | `productionInput` in `apps/web/lib/apps-view.ts` | Today it is a vertical ProductPromo in the reader's language with the three controls visible, which is defensible and is the owner's to confirm. The other half of this question — whether adopting an earlier home directory copied or moved — went away with the feature on 10-Sep-2026 | **the user** |
| The two process guardians are JavaScript inside a string | `PROCESS_GUARDIAN` in `packages/apps/src/process.ts`, `APP_GUARDIAN` in `apps/web/lib/app-client.ts` | They are what owns the process group of npm and of the app, and they are spawned with `node -e`. As files they would read better and be checked by the compiler; as files they also have to survive `build:app`, and whether `import.meta.url` still points at them once Next has traced the package is precisely the kind of thing this repository has been bitten by before, with no test that would catch it. What removes the real hazard —a typo that only shows at runtime— is already there: `process.test.ts` and `app-client.test.ts` start both of them for real and count the processes left behind. Moving them is worth doing the day `build:app` grows a test that spawns one | **decided** |
| The tarball's size and the TSX hook, which were measured | `@panoma/video`'s `package.json` and `dist/` | Closed rather than pending, and written down because the shape returns: the packed artifact is 61 entries and about 2.2 MB, far under the 30 MB that would have opened a conversation, and the scenes are precompiled so `typescript` does not travel as a runtime dependency | **decided** |
| The agent's door has started no real production | `panoma_video` in `packages/mcp/src/index.ts`; `POST /api/agent/video` | On 12-Sep-2026 the four tools were driven end to end against a catalog with the app installed and ready: the list, a finished run read whole with its twelve stages and the spend, the refusals (`local-url-required` through the real `enqueueAppJob`, an unknown id, an unknown folder). What was not asked for is a run that renders, because on that machine the app's settings had a model and a voice on, and a production started to prove a route would have spent the person's calls — which is the line the door itself draws. The row it would write is the row the route tests write, through the same `enqueueAppJobRow`, and the run itself is the same job the screen starts; what a real one would add is a measurement of the `wait` loop over minutes rather than over a mocked notice | **the user** — one `panoma_video` from a connected agent; `until: "plan"` keeps it to seconds, and the model stays off only while the app's setting says `none` |

## The handoff, checked on 11-Sep-2026, and its agent channel on 12-Sep-2026

The feature was designed against four agents on one Mac and reviewed by three critics before a
line was written ([handoff.md](handoff.md)); the two desktop apps were added the same evening,
and the two MCP tools the next day. What the review left knowingly open, and what it closed
with a reason, is here so that nobody "finishes" an open row by guessing, or reopens a decided
one by mistake.

| what | where | why it is still like this | who decides |
| --- | --- | --- | --- |
| The Gemini CLI writer has never been run against a live Gemini CLI | `packages/handoff/src/writers/gemini.ts`, `stores/gemini.ts`; `testedWith` in `src/fidelity.ts` | Gemini CLI is not installed on the machine the feature was built on, so the store shape —`~/.gemini/tmp/<project id>/chats/session-*.jsonl`, the `projects.json` slug or the sha256 of the folder, the `{"$set":{"summary"}}` record— was read from the CLI's source and never from a file it wrote. The writer's own test proves only that the reader reads back what it wrote. The first person with `gemini` installed who runs `gemini --resume` on a file panoma wrote is the first real test, and until then that row of the fidelity table has no version and no date — `testedWith` is data the screen paints as it is, since 12-Sep-2026 — and each surface words the absence in its own sentence («never run live») | **open** |
| A Codex copy's settings line stamps the person's config, and how the desktop app reads it is unmeasured | `threadSettingsApplied` in `packages/handoff/src/writers/codex.ts`; `codexModelSettings` in `stores/codex.ts` | The tail line stamps `approval_policy` and a `permission_profile` built from `sandbox_mode`, both from the person's own `config.toml` with Codex's defaults (`on-request`, `workspace-write`) where it says nothing; until 12-Sep-2026 it stamped `never` and a disabled sandbox on every copy. Whether the app re-derives both from the config when a thread runs or reads them off that line is not known without the app's source, so the writer promises the one thing it can: a copy never says more than the config allows. Measuring it means a Codex app with a `workspace-write` config and a handed-off thread that runs a command | **open** |
| OpenCode's store on Windows | `packages/handoff/src/stores/opencode.ts` | The store is `$XDG_DATA_HOME/opencode/opencode.db`, and `XDG_DATA_HOME` has no conventional value on Windows: what OpenCode falls back to there was not measured, and guessing `%LOCALAPPDATA%` would make discovery report "not found" with confidence on the one system where it might be somewhere else. On this Mac the store is at the documented default, `~/.local/share/opencode`; whatever `stores/opencode.ts` resolves on `win32` with the variable unset is a reading of the source, not a folder anybody opened | **open** |
| Cursor, Copilot and the other document-only agents get a document, not a conversation | `fidelityOf` in `packages/handoff/src/fidelity.ts`; `NATIVE_AGENTS` in `src/types.ts` | Their stores were not read, and their resume is not a file in a folder: Cursor keeps one SQLite database per workspace and Copilot's CLI has no published resume door. The door that exists for every agent is its first message, so `brief` writes the digest and the last exchange as a document to paste, and the screen says on that row that the agent cannot resume a written conversation. Reading their stores as sources is a second feature, decided when somebody asks for it and not before | **decided** |
| Live resume is watched by no test, in any of the four agents, and each row has a version | `testedWith` in `packages/handoff/src/fidelity.ts`: Claude Code 2.1.258, Codex CLI 0.153.0, OpenCode 1.2.6, Gemini CLI none | The package's tests prove the round trip inside panoma —what a writer writes, its reader reads back with the same hash— and that is deliberate: a test that ran the real binary would open a network conversation on every run and would need the four agents installed on CI. So a new release of any of them can change its store and nothing here goes red; the versions in the table are the ones checked by hand, and re-checking is the manual step of every release that touches this package. A pinned-version CI job with the four binaries is the remedy, and nobody has written it | **open** |
| `redactSecrets` has no entropy net, so a secret with no known shape travels in a handoff | `packages/core/src/redact.ts`; `dropped.secrets` in `packages/handoff` | Eleven provider shapes and one visible mark, the same as everywhere the house redacts, and the argument is [secrets.md](secrets.md)'s: an entropy heuristic sooner or later eats a sha, a URL or a base64 image, and a transcript is made of exactly those. The cheap failure is a password that slips through into a file that is already on the same disk in the clear; the expensive one is a copy so riddled with marks that the person turns the redaction off. The count on the screen is of what matched and says nothing about what did not, and the page says so in as many words | **decided** |
| The two desktop deep links can change with an app update, and nothing here goes red | `APP_URL` and `resumeInApp` in `packages/handoff/src/fidelity.ts`; the launch route's allowlist in `apps/web/app/api/handoff/launch/route.ts` | `claude://resume?session=<id>` and `codex://threads/<id>` were read from the two app bundles and run once each on 11-Sep-2026, against Claude.app 1.52386.3 and the Codex app bundled in ChatGPT.app (app-server 0.154.0-alpha.6). Neither vendor documents the scheme; the Codex one is what the app's own «Copy link» writes, which is the nearest thing to a promise. A test holds the templates as closed strings, so panoma cannot drift, but the app can, and the first sign will be a link that opens the app and nothing else. That is why the `open -a <bundle>` fallback and the CLI resume line are printed under the button every time: they resume the same file without the link. Re-running the two links is the manual step of every release that touches the writers, next to the live-resume row above | **open** |
| The Claude sidebar shows an un-adopted file only for 48 h; the picker and the link always do | `readers/claude.ts` and `writers/claude.ts` in `packages/handoff`; «The desktop apps» in [handoff.md](handoff.md) | Claude.app's Code sidebar lists CLI transcripts it discovers on disk only while their mtime is under two days old, and a file panoma wrote is one of those until the app adopts it. Its resume picker has no age cap, and `claude://resume?session=<id>` finds the file at any age, so a handoff written «for later» never disappears, it only leaves the sidebar. Chasing it —touching the mtime, stamping `entrypoint: "claude-desktop"`, writing the app's own record— was each considered and refused on 11-Sep-2026: the first lies about the file, the second hides it from the very list (verified), the third writes a folder panoma never opens. The receipt points at the link, not at the sidebar, and the screen says so | **decided** |
| `open -a Claude <folder>` lands in Cowork on current builds, and the open-all desktop family still uses it | `openApp()` in `apps/web/lib/open-targets.ts`; [open-all.md](open-all.md) | Seen on 11-Sep-2026 on Claude.app 1.52386.3: the Electron open-file path routes a dropped folder to the Cowork tab when that feature is on, as it is on this Mac, and no Code session appears; `claude://code/new?folder=<path>` is what opens the Code tab on a folder, read from the bundle and not run. The handoff does not use `open -a` for a conversation, it uses the deep link with `open -a` as the fallback, so it is unaffected. The desktop row of «Open everything» is affected and was left as it is: `openApp()` refuses any scheme on purpose, its comment says why, and changing what a saved plan does with the Claude button is a decision about the open-all family, not about the handoff | **open, the user decides** |
| The model-written digest cannot be ordered through the agent channel | `POST /api/agent/handoff` refuses a body with `digestBy`; `panoma_handoff` in `packages/mcp/src/index.ts` has no such field | [budgets.md](budgets.md) holds the `handoff` family to «one action, by the person, on one transcript», and its cap of ten is there to brake the one loop that could form; an agent calling a tool is that loop, and the person's press is what the cap counts. The mechanical digest is free and identical on every run, so an agent loses nothing it could not get by asking the person to tick the box. Reopening it means reopening the budgets record —a second family, or a per-agent share of this one— and it is the owner's decision, not a route's | **decided, the user reopens** |
| The same agent, at any tier, is refused on the agent channel | `POST /api/agent/handoff` answers 409 `same-store` for the source's own agent whatever the tier and the surface, with a hint that sends the person to the screen or the terminal | The person's surfaces allow the same agent at `full` (the same file, nothing written) and at `compact` (a shorter copy), because the steps around either are the person's: sign out, sign in, resume. A model with a shell must never be handed a sign-out and a sign-in command, and a `compact` copy for the other account is meaningful only next to those steps, so the channel refuses the whole case rather than half of it. Allowing `compact` alone —a copy with no account steps printed— is the one variant that was weighed and refused on 12-Sep-2026, because the copy would sit in the store with nobody told what it is for. The legal line decides, and the owner reads it | **decided, the user reopens** |
| Whether a handoff an agent orders should also write a journal entry | `recordHandoff` in `packages/db/src/handoffs.ts`; `panoma_log` in `packages/mcp/src/index.ts` | Today the act leaves one line: the receipt, with `requested_by`. It leaves no `agent_journal` row, so «since yesterday» in the next `panoma_context` will not say that this agent handed the conversation to another, unless the agent also calls `panoma_log`. Writing the entry from the route would put a journal row behind an act the agent may not think of as work, and the journal is what the memory distiller reads; not writing it leaves the receipt as the only trace, which the Handoff screen lists and the briefing does not. Neither was measured against a real week of use | **open** |

## What it does not do / Known limits

- **It is not a complete inventory of debt.** It is the list of what has to be read before
  touching. Whatever is not here and looks wrong, probably is.
- **No test watches this list.** A row that gets fixed stays written as if it were still open,
  which is exactly the failure this page hunts down in other documents. When you close one,
  delete it.
- **The rows that say "the user" are not up for grabs.** They are not pending work waiting for
  somebody with time: they are decisions that belong to another person.
- **The figures are from 25-Aug-2026, those of the spend and dependency tables from 6-Sep-2026,
  those of the apps table and the stylesheet table from 8-Sep-2026, those of the handoff
  table from 11-Sep-2026 and its three agent-channel rows from 12-Sep-2026, and those of the four
  memory tables — the contract, the capture, the checks and the Twin's learning — from
  14-Sep-2026**, and were checked by reading the code, not by running anything. Two tables are the exception in that direction. The
  stylesheet one: its contrasts were measured with the sheet's own routine, and the counts of
  `text-faint` and of `font-display` by reading every `.tsx`. Those two are the volatile figures on
  this page — they move with every component that is written, and they moved on 8-Sep-2026 while
  the catalog launcher was being rewritten. The contrast ratios beside them do not move that way:
  a test re-measures those on every run. And the apps one: its sizes were
  measured on this disk, and what it says nobody has run is exactly what nobody has run. The two that a
  test really measures are the contrasts and the absence of `setVerdictAccepted`; the SQL in the
  spend table was written to be run, not run. The dependency table is the exception: its
  advisories were read from OSV and from GitHub, and the fix was packed and installed.
